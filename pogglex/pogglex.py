# Lexer and factory methods
import typing

from pogglex.abstracts import LexObj

# Builds regular expressions from the definitions supplied
# in the module


def lexer_factory(lex_defs: LexObj):
    pass
